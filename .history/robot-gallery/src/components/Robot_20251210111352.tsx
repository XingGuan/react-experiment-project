import React from "react";

//这里 FC = functional component 函数式组件，React.FC 这里代表函数式组件的接口
// type FC<P = {}> = React.FunctionComponent<P> 接受一个泛型参数P
const Robot: React.FC = (props) => {
  return <div></div>;
};

export default Robot;
